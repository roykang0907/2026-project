# 개발 설계 문서 (HANDOFF)
## AI 기반 학부모 민원 대응 보조 시스템

> 이 문서는 **개발 레포**에 넣는 핸드오프 문서다.
> 새 Claude Code 세션은 이 문서 하나로 프로젝트 맥락 전체를 파악한다.
> 연구 근거·통계 원본은 별도 "자료 레포"(`2026-project`)에 있으며, 여기엔 옮기지 않는다.

---

## 0. 한 줄 요약

학부모 악성 민원으로부터 **교사를 보호**하고, 그 결과 **학생의 학습권을 회복**하는 것을 목표로 하는, **로컬 추론 기반 3단계 AI 시스템**.

핵심 메시지: *"교사를 보호함으로써 학생의 학습권을 회복한다."*

---

## 1. 시스템 구조 (3단계)

이미지의 흐름: 학부모 민원 제기 → 수신 → **공격성 판단** → 위험도 분기 → **감정 순화 + RAG** → 순화 답변 생성 → **비동기 대시보드로 교사에게 전달**.

| 단계 | 이름 | 하는 일 | 모델/기술 |
|------|------|---------|-----------|
| **1단계** | 공격성 판단 엔진 | 민원 텍스트 → **정상 / 주의 / 위험** 3클래스 분류. 폭언·협박 수치화 | **KoBERT 파인튜닝** ⭐ (+ 긴급 키워드 룰) |
| **2단계** | 감정 순화 + RAG | 공격성·감정 제거 후 핵심 요구만 공적 표현으로 변환. 법률 DB 검색해 교육청 지침 매칭 | Llama 3.x 또는 Qwen2.5 (QLoRA) + RAG(pgvector) |
| **3단계** | 비동기 대시보드 | 수업 종료 후 일괄 전달. 교사가 답변 작성 시 위험 표현(과실 인정 등) 실시간 경고 | React 웹앱 + 룰 |

### 데이터 흐름
```
[학부모 민원]
   ↓
[1단계 KoBERT 분류]  → 카테고리 + 공격성(정상/주의/위험) + 긴급 플래그
   ↓
   위험도 ≥ 임계? ─YES→ 악성/주의 민원으로 분류
   │             ─NO──→ 일반 민원
   ↓
[2단계 순화 LLM + RAG]  → 순화본 + 메타데이터(반복성/강도/원어조) + 근거 지침
   ↓
[3단계 교사 대시보드]  → 순화 카드(기본) / 원본 토글 / 우선순위 큐 / 답변 코칭
   ↓
[학부모에게 발송] + [내부 기록 자동 저장]
```

---

## 2. 확정된 핵심 설계 결정

1. **순화 뷰 기본 + 원본 항상 열람 가능** — 정보 은폐가 아니라 우선순위 변경. (완전 은폐는 법적·윤리적 리스크라 폐기)
2. **긴급 분기** — 안전·법적 사안(학폭, 아동학대, 자해, 고소 등)은 룰로 즉시 위험 상단 노출. 진짜 긴급 민원이 필터링되는 사고 방지.
3. **로컬 추론 (외부 API 미사용)** — 학부모 민원은 개인정보. 외부 API 전송 시 개인정보보호법 이슈. 또한 공모전에서 "자체 학습 모델"이 "API 래퍼"보다 점수 우위.
4. **메타데이터 보존** — 반복성·강도·원어조를 분리 저장해 가스라이팅/반복 민원 패턴 탐지 가능.
5. **AI 제안 disclaimer 고정** — "AI 제안, 최종 책임은 교사" UI에 항상 표시.

---

## 3. 기술 스택

### 환경 / 운영
- **모델 학습**: 성능 좋은 **맥북**에서 수행 (Apple Silicon).
- **코딩**: GitHub Desktop으로 **윈도우 ↔ 맥 동기화**하며 작업.
- **추론**: 로컬 (MLX 1순위 또는 Ollama). 무거운 학습은 git에 올리지 않고, 코드/노트북 골격만 동기화.

### 모델
| 용도 | 모델 | 비고 |
|------|------|------|
| 1단계 공격성 분류 | **KoBERT 파인튜닝** | AI Hub 감성 대화 말뭉치. (여유 시 KoELECTRA와 F1 비교 = 가산점) |
| 2단계 순화 변환 | Qwen2.5-14B 또는 Llama 3.x + QLoRA | Apache 2.0, 한국어 우수 |
| 3단계 답변 보조 | 위 모델 + RAG | 32B까지 맥북 감당 가능 |

### 백엔드
- FastAPI (Python 3.11)
- PostgreSQL + pgvector (RAG)
- WebSocket (실시간 민원 푸시)

### 프론트엔드
- Vite + React + TypeScript
- Tailwind + shadcn/ui
- Zustand (상태), React Query (서버 상태)
- Tiptap (답변 편집기)

### ML / 파인튜닝 도구
- transformers + PEFT + TRL (+ Unsloth)
- KoBERT: `skt/kobert-base-v1` + `kobert_tokenizer` (또는 `monologg/kobert`)
- QLoRA 4-bit (r=16, alpha=32 기준선)

---

## 4. 데이터 전략

### 1단계 (공격성 분류) — KoBERT
- **AI Hub 감성 대화 말뭉치** 사용 (※ 아직 신청 전 → **가장 먼저 신청**, 승인 1~3일).
- 감정 라벨 → 3클래스 매핑:
  - 분노/혐오/공격 → **위험**
  - 불만/슬픔/불안 → **주의**
  - 중립/기쁨 → **정상**
- 8:1:1 (train/val/test) 분할.
- **긴급 키워드 룰 별도** (학폭, 아동학대, 자해, 고소 등) — 분류 결과와 무관하게 우선순위 상단.
- 평가지표: **Macro-F1** + 혼동행렬. *위험을 정상으로 놓치는 오류(false negative)를 최소화하는 게 가장 중요.*

### 2단계 (순화 변환)
- **AI Hub "공적 말하기 실습 및 평가 데이터"** 사용 — 공적/격식 있는 말하기 표현 학습의 핵심 소스. (※ AI Hub 신청 시 함께 신청)
- 학부모↔교사 도메인의 직접 데이터는 부족하므로, 위 데이터로 "공적 말하기" 문체를 학습하고 부족분은 **합성 데이터(공격 민원 ↔ 공적 표현 쌍)로 보완**.
- 수작업 검수 100건 = 골든셋.
- "데이터 한계 인지 + 공적 말하기 데이터 + 합성 데이터로 보완" 서술은 오히려 가산점.

### 3단계 (RAG 문서)
- 교원지위향상법, 교육공무원법, 학교폭력예방법
- 교육부·시도교육청 민원 응대 매뉴얼, 교권보호위 사례집(공개분)
- 청크 분할 → 임베딩 → pgvector 저장.

---

## 5. 단계별 실행 계획 (Phase)

### Phase 0 — 준비 (1~2일)
- [ ] 개발 레포 모노레포 구조: `ml/` `backend/` `frontend/` `docs/`
- [ ] Python 3.11 + Node 20 환경
- [ ] **AI Hub 데이터셋 신청** (가장 먼저, 승인 대기 동안 다른 작업)
  - 1단계용: **감성 대화 말뭉치** (공격성 분류)
  - 2단계용: **공적 말하기 실습 및 평가 데이터** (순화 변환)
- [ ] 맥북에 MLX/Ollama 설치
- [ ] `.gitignore` (모델·대용량 데이터 제외)

### Phase 1 — KoBERT 공격성 분류기 (1단계) ⭐ 지금 시작 지점
- 1-1. 라벨 매핑 + 긴급 키워드 룰 사전
- 1-2. **더미 데이터로 도는 학습→예측 파이프라인 먼저 완성** (AI Hub 승인 대기용)
- 1-3. 승인 후 실제 데이터로 KoBERT 파인튜닝 (맥북, 2~4시간)
- 1-4. 평가(Macro-F1) + 서빙 준비(ONNX 또는 transformers)

### Phase 2 — 순화 + RAG (2단계)
- 합성 데이터 생성 → QLoRA 파인튜닝 → RAG 구축

### Phase 3 — 백엔드 파이프라인 (3~5일)
- FastAPI: 수신 → 분류 → 순화 → 저장 → 조회 → 답변 제안/검증
- PostgreSQL + pgvector

### Phase 4 — 프론트엔드 비동기 대시보드 (3단계)
- 순화 카드 + 원본 토글 + 우선순위 큐 + 답변 위험 표현 빨간줄 + disclaimer

### Phase 5 — 통합·시연·발표 (1주)
- E2E 데모, 정확도 측정표(분류 F1/순화 평가/RAG 적중률), 시연 영상, 발표자료

---

## 6. 현재 상태 / 다음 작업

**현재 상태 (2026-05-31 기준)**
- 연구·통계 근거 분석 완료 (가설 1·2 검증됨 — 자료 레포 참조).
- 설계 확정. **코드는 아직 0**.
- AI Hub 데이터셋 **미신청**.

**지금 당장 할 일**
1. **AI Hub 감성 대화 말뭉치 신청** (승인 시간 벌기)
2. 개발 레포 폴더 구조 + `.gitignore`
3. **KoBERT 1단계: 더미 데이터로 도는 학습 파이프라인 골격 작성** ← 첫 코드

---

## 7. 윤리·법적 안전장치 (발표 강조 포인트)
1. 순화 뷰 기본 + 원본 항상 열람 가능 (은폐 아님)
2. 긴급도 분기 (안전·법적 사안 즉시 노출)
3. 메타데이터 보존 (반복 민원·가스라이팅 패턴 탐지)
4. AI 제안 disclaimer 고정 ("최종 책임은 교사")
5. 로컬 처리 (민원이 외부 서버로 안 나감)

---

## 8. 참고: 자료 레포 위치

연구 근거·통계·논문은 별도 자료 레포(`2026-project`)에 있다. 발표자료의 문제정의·통계 근거 섹션에 활용:
- `학부모_악성민원_연구_종합.md` — 가설 검증 종합
- `프로그램_문서.md` — 기능 상세 설계 (감정 필터/답변 코칭/자동 분류)
- `data/` — 빅카인즈 7,957건/84개월 + 통계 CSV 10종
- `그래프1~7_*.png` — 분석 시각화

> 학습용으로 필요한 CSV만 그때그때 개발 레포로 가져온다 (대용량 원본은 복사 X).
