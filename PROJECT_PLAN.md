# 교사 민원 격리·순화 시스템 — 설계 대화 기록

> 2026-05-24 작성. Claude Code 세션에서 ROY와의 설계 논의 정리.

---

## 1. 프로젝트 개요

학부모 악성 민원으로부터 교사를 보호하는 **3-Layer 시스템**.

| Layer | 기능 | 목적 |
|---|---|---|
| **Layer 1 — 격리/분류** | 들어오는 민원을 AI가 카테고리·공격성·긴급도로 분류 | 교사의 인지 부담 감소 |
| **Layer 2 — 감정 순화** | 학부모의 공격성·과장·감정 표현 제거, 핵심 요구만 공적 표현으로 변환 | 교사 정신건강 보호 |
| **Layer 3 — 답변 보조** | 법률/교육청 문서 기반 답변 제안 + 교사 답변의 위험 표현 실시간 검출 | 법적 안전성, Hallucination 최소화 |

---

## 2. 핵심 설계 결정

### 2-1. 민원 격리 정책: **순화 뷰 기본 + 원본 열람 가능**

- 처음 설계는 "교사가 알림 존재 자체를 모름" (완전 은폐)이었으나, 법적/윤리적 리스크 회피 위해 변경.
- 모든 민원의 존재는 교사가 인지하되, **기본 뷰는 순화된 버전**.
- 원본은 "원본 보기" 토글로 항상 접근 가능.
- 안전·법적 사안(학교폭력, 아동학대 신호 등)은 우선순위 큐 상단으로.

**이유**: 학부모 민원은 행정 기록 대상이며, 자녀 안전 관련 진짜 긴급 민원이 필터링되는 사고를 막아야 함.

### 2-2. MVP 범위: **Full 3-Layer 전부 구현**

- 시연 풍성도·출품작 임팩트 우선.
- 단, 데이터 품질·재학습 루프에 충분한 시간 확보 필요.

### 2-3. 모델 운영: **로컬 추론 (외부 API 미사용)**

- "Claude Max / ChatGPT Max"는 구독제이므로 웹앱에 사용 불가. API는 토큰당 과금.
- 개인정보(학부모 민원)를 외부 API로 보내면 개인정보보호법 이슈.
- 출품작 관점에서 "AI Hub 데이터 + 자체 학습 모델"이 "API 래퍼"보다 점수 우위.
- Layer 3(답변 보조)도 로컬 LLM + RAG로 처리.

---

## 3. 기술 스택

### 하드웨어
- **로컬 추론**: 맥북 M5 Max 128GB (사실상 모델 크기 제약 없음)
- **파인튜닝**: Colab Pro (A100/L4)

### 모델
| 용도 | 모델 | 비고 |
|---|---|---|
| Layer 1 공격성 분류 | KoBERT 또는 KoELECTRA 파인튜닝 | AI Hub 감성 대화 말뭉치 |
| Layer 2 공적 말하기 변환 | **Qwen2.5-14B-Instruct** + QLoRA | Apache 2.0, 한국어 우수 |
| Layer 3 답변 보조 | Qwen2.5-14B 또는 32B + RAG | 32B까지 M5 Max 감당 가능 |
| 양자화 | Q6_K 또는 Q8_0 | 128GB RAM 활용, 품질 손실 최소 |
| 추론 엔진 | MLX (1순위) 또는 Ollama | MLX가 Apple Silicon에서 가장 빠름 |

### 백엔드
- FastAPI (Python 3.11)
- PostgreSQL + pgvector (RAG)
- WebSocket (실시간 민원 푸시)

### 프론트엔드
- Vite + React + TypeScript
- Tailwind + shadcn/ui
- Zustand (상태), React Query (서버 상태)
- Tiptap (답변 편집기)

### 파인튜닝 도구
- Unsloth + Transformers + PEFT + TRL
- QLoRA 4-bit (r=16, alpha=32 기준선)

---

## 4. 데이터 전략

### Layer 1 (공격성 분류)
- **AI Hub 감성 대화 말뭉치**
- 분노/공격성 → "공격적", 중립/슬픔 → "비공격적"
- 긴급 키워드 룰 별도 (학폭, 아동학대, 자해 등)

### Layer 2 (공적 말하기 변환) — **가장 큰 병목**
- AI Hub 공공행정/민원 응대 말뭉치 탐색
- 학부모↔교사 도메인 데이터는 부족 예상
- **합성 데이터 1000건 목표**: Claude/GPT API로 (공격 민원 ↔ 공적 표현) 쌍 생성
- 수작업 검수 100건 = 골든셋

### Layer 3 (RAG 문서)
- 교원지위향상법, 교육공무원법, 학교폭력예방법
- 교육부·시도교육청 민원 응대 매뉴얼
- 교권보호위원회 사례집 (공개분)

### 주의사항
- 학부모↔교사 도메인 데이터 부족은 "데이터 한계 인지 + 합성 데이터로 보완" 서술이 오히려 가산점.
- AI Hub "감성대화 말뭉치"는 일상 대화 기반이므로 도메인 어댑테이션 필요.

---

## 5. 아키텍처 다이어그램

```
[학부모 민원 입력]
      ↓
[Layer 1: 분류기]  ← KoBERT (로컬)
   - 카테고리(학습/생활/안전/행정/기타)
   - 공격성 점수
   - 긴급도 플래그(키워드 룰)
      ↓
[Layer 2: 순화 LLM]  ← Qwen2.5-14B QLoRA (로컬)
   - 공격성 제거, 공적 말하기 변환
   - 메타데이터 보존(반복성, 강도, 원어조)
      ↓
[교사 대시보드 (React)]
   - 기본: 순화 카드 뷰
   - "원본 보기" 토글
   - 우선순위 큐(긴급도순)
      ↓
[Layer 3: 답변 편집기]
   - Tiptap 에디터
   - 정규식 룰: 감정/법적 위험 문구 빨간줄
   - LLM 제안: RAG(교육청 문서, 법률)
   - "AI 제안, 최종 책임은 교사" disclaimer 고정
```

### React ↔ 백엔드 ↔ 모델 흐름
```
React (Vite)  ──HTTP──▶  FastAPI 백엔드  ──HTTP──▶  Ollama/MLX (localhost)
                              │
                              ├─▶ KoBERT 분류기 (로컬)
                              └─▶ pgvector RAG
```

---

## 6. 단계별 실행 계획

### Phase 0 — 준비 (1~2일)
- [ ] 모노레포 구조 (`frontend/`, `backend/`, `ml/`, `data/`, `notebooks/`)
- [ ] Python 3.11 + Node 20 + pnpm/uv
- [ ] **AI Hub 회원가입 + 데이터셋 신청** (승인 1~3일, 가장 먼저)
- [ ] Colab Pro 확인, Google Drive 마운트 테스트
- [ ] 맥북에 Ollama/MLX 설치 + Qwen2.5-14B 베이스 다운로드
- [ ] GitHub `.gitignore` (모델·데이터 제외)

### Phase 1 — 데이터 확보 (1~2주)
- **1-1. 공격성 분류 데이터**: AI Hub 감성대화 다운로드, 라벨 매핑, 8:1:1 분할
- **1-2. 공적 말하기 변환 데이터**: 합성 1000건 + 검수 100건 골든셋
- **1-3. RAG 문서**: 법률·매뉴얼 PDF 수집, 청크 분할, 임베딩 준비

### Phase 2 — 모델 학습 (Colab, 1~2주)
- **2-1. KoBERT 분류기**: 학습 2~4시간, ONNX 변환
- **2-2. Qwen2.5-14B QLoRA**: 학습 4~6시간 (1000건 3 epoch 기준), 1~2회 재학습 각오

### Phase 3 — 로컬 배포 (맥북, 3~5일)
- Qwen2.5-14B 병합 모델 → MLX 또는 GGUF(Q6_K) 변환
- 벤치마크 목표: 첫 토큰 1초 이내, 30 tok/s 이상
- FastAPI 엔드포인트:
  - `POST /api/complaints/ingest` — 민원 수신 → 파이프라인
  - `GET /api/complaints` — 대시보드 목록 (우선순위 정렬)
  - `GET /api/complaints/{id}` — 순화본 + 원본 + 메타
  - `POST /api/draft/suggest` — RAG + LLM 답변 제안
  - `POST /api/draft/validate` — 위험 표현 검출
- PostgreSQL + pgvector docker-compose
- 정규식 룰 사전 100~200개

### Phase 4 — 프론트엔드 (1~2주)
- Vite + React + TS + Tailwind + shadcn/ui
- **대시보드**: 순화 카드 + 우선순위 배지 + 원본 토글 + 카테고리 필터
- **민원 상세**: 순화본/원본 좌우 비교 + 메타데이터
- **답변 작성**: Tiptap + 실시간 빨간줄 + AI 제안 + disclaimer 고정
- 데모 시드 5~10개

### Phase 5 — 통합·시연·발표 (1주)
- E2E 시나리오 검증
- 정확도 측정표 (분류 F1, 변환 평가, RAG 적중률)
- 시연 영상 녹화
- 발표 자료 + README

### 전체 일정
| 시나리오 | 소요 |
|---|---|
| 풀타임 1명 | 6~8주 |
| 학교/직장 병행 | 3~4개월 |

---

## 7. 윤리·법적 안전장치 (발표 시 강조 포인트)

1. **순화 뷰 기본 + 원본 항상 열람 가능** — 정보 은폐 아닌 우선순위 변경
2. **긴급도 분기** — 안전·법적 사안은 즉시 원본 노출
3. **메타데이터 보존** — 반복성·강도·원어조를 분리 저장하여 패턴 탐지 가능 (가스라이팅, 반복 민원)
4. **AI 제안 disclaimer** — "최종 책임은 교사" UI에 고정
5. **로컬 처리** — 학부모 민원이 외부 서버로 전송되지 않음 (개인정보보호)

---

## 8. 출품작 어필 포인트

| 항목 | 강점 |
|---|---|
| 사회적 임팩트 | 교권 침해 통계로 뒷받침, 시의성 있음 |
| 데이터 활용 | AI Hub 활용은 공모전 가산점 (한국 심사 기준) |
| 자체 모델 학습 | "GPT 래퍼" 비판 회피, 기술 깊이 확보 |
| 통합 파이프라인 | 3-Layer 분리로 모듈별 평가·시연 용이 |
| 윤리 안전망 | 순화 뷰 + 원본 열람 + disclaimer로 심사 질문 방어 |
| 온디바이스 | 맥북 한 대에서 모든 게 돌아감 → 개인정보 안전 어필 |

---

## 9. 다음 작업 (오늘 당장 할 일)

1. **AI Hub 데이터셋 신청** (승인 시간 벌기)
2. **저장소 구조 만들기** (Phase 0)
3. **합성 데이터 생성 스크립트 작성 시작** (Phase 1-2)

---

## 10. 미해결/확정 필요 항목

- [ ] AI Hub 어떤 데이터셋이 실제로 사용 가능한지 (회원가입 후 확인)
- [ ] RAG용 법률·매뉴얼 문서 수집 방법 (수동 vs 크롤링)
- [ ] 합성 데이터 생성 시나리오 다양성 확보 방안
- [ ] 시연 환경 (오프라인 데모 가능하도록 준비)
- [ ] 발표 시 "AI 학습 데이터에 실제 학부모 발화 없음" 명시 여부

---

## 부록: 기존 자료

프로젝트 폴더에 이미 있는 자료:
- `학부모_악성민원_연구_종합.md`
- `학부모_악성민원_교사_학생_영향_조사.pdf`
- `교육활동침해 실태조사 통계.xlsx`
- `가설_검증_설계.pdf`
- `그래프1~7_*.png` (분석 그래프)
- `논문_자료/`, `엑셀 자료/`

→ 발표 자료의 **문제 정의·통계 근거** 섹션에 활용 가능.
